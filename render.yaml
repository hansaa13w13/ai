services:
  - type: web
    name: bist-predator
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: starter
    envVars:
      - key: PORT
        value: 5000
      - key: DATA_DIR
        value: /data/cache
    disk:
      name: predator-data
      mountPath: /data
      sizeGB: 1
    healthCheckPath: /?action=daemon_status
